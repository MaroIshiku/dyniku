apiVersion: apps/v1
kind: Deployment
metadata:
  name: dyniku
  labels:
    app: dyniku
spec:
  selector:
    matchLabels:
      app: dyniku
  template:
    metadata:
      labels:
        app: dyniku
    spec:
      containers:
        - name: dyniku
          image: ${DYNIKU_IMAGE:-ghcr.io/maroishiku/dyniku:0.1.0}
          envFrom:
            - secretRef:
                name: dyniku-config
          ports:
            - containerPort: 8507
