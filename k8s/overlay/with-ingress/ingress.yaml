apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: dyniku
spec:
  rules:
    - host: localhost
      http:
        paths:
          - path: /
            pathType: ImplementationSpecific
            backend:
              service:
                name: dyniku
                port:
                  name: http
